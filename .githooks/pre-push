#!/bin/sh
# Bloqueia o push se o schema do banco divergir do snapshot commitado.
npm run --silent moas:check || {
  echo ""
  echo "  O schema do banco mudou e docs/db/ não foi atualizado."
  echo "  Rode:  npm run moas"
  echo "  Depois:  git add docs/db && git commit"
  echo ""
  exit 1
}
